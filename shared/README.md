# ERP Shared Library

Additive shared primitives for cross-service consistency:

- `common`: DTOs and API envelopes
- `events`: event naming/contract helpers
- `auth`: RBAC and permission decorators
- `constants`: service and domain constants

This folder is intentionally non-breaking and can be imported incrementally by services.
